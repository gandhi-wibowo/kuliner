1. cek jaringan
2. login : true => email,password : true => (response)simpan ke sharedpref => masuk ke tampilan admin
	 : false => form daftar : nama_user,email_user,hp_user,password_user => (response) simpan ke shared => 
		cek_login : true => masuk ke tampilan admin
3. kuliner : kalau belum ada => tampilkan form tambah kuliner
	   : kalau sudah ada => tampilkan form edit kuliner
4. menu : tambah menu, edit menu, hapus menu
5.  
